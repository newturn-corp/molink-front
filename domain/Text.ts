export default class Text {
    title: string
    order: number
    depth: number

    constructor (title: string, order: number, depth: number) {
        this.title = title
        this.order = order
        this.depth = depth
    }
}
